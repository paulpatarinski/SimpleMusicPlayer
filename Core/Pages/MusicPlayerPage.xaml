<?xml version="1.0" encoding="utf-8" ?>
<base:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
					   xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
					   xmlns:base="clr-namespace:Core.Pages.Base;assembly=Core"
					   xmlns:codes="clr-namespace:Core.Helpers.Codes;assembly=Core"
					   x:Class="Core.Pages.MusicPlayerPage">
<Grid BackgroundColor="{x:Static codes:Colors.Black}" Padding="0,5,0,0">
  <Grid.RowDefinitions>
    <RowDefinition Height="0.2*"/>
    <RowDefinition Height="0.6*"/>
    <RowDefinition Height="0.2*"/>
  </Grid.RowDefinitions>
  <Grid.ColumnDefinitions>
    <ColumnDefinition Width="*"/>
  </Grid.ColumnDefinitions>
  <Grid Grid.Row="0" Grid.Column="0">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="0.1*"/>
      <ColumnDefinition Width="0.9*"/>
    </Grid.ColumnDefinitions>
    <StackLayout Grid.Row="0" Grid.Column="1" Spacing="0">
      <Label Text="{Binding MusicFile.ArtistName}" TextColor="{x:Static codes:Colors.Gray}"/>
      <Label Text="{Binding MusicFile.SongTitle}" TextColor="{x:Static codes:Colors.White}" Font="{x:Static codes:Fonts.LargeBold}"/>
      <Label Text="{Binding MusicFile.AlbumName}" TextColor="{x:Static codes:Colors.Gray}"/>
    </StackLayout>

  </Grid>
  
</Grid>
  
</base:BasePage>